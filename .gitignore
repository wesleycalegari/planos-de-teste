# ── Scripts de automação — migrados para o repositório privado ─────────────
# testes-automatizados/ não deve mais viver aqui (repo público) — pode conter
# credenciais de ambiente HML. Local correto: c:\projetos\satelites citweb\testes-automatizados\
# Migração feita em 14/07/2026 (ver memória project_comparativo_fluxo_qa).
testes-automatizados/

# ── Credenciais / configuração local ───────────────────────────────────────
.env
.env.*
